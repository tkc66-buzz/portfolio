# Contracts

This feature is UI-only (in-page navigation + styling).

- No API endpoints
- No backend services
- No contract tests

Validation:
- Manual scroll + click checks
- `pnpm lint`
- `pnpm build`


