# Implementation Plan: Add CodeZine Article Series

**Branch**: `041-add-codezine-series` | **Date**: 2026-02-07 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/041-add-codezine-series/spec.md`

## Summary

Add an "Articles" group to the Activities section to accommodate the user's CodeZine serialized article on Platform Engineering. This requires extending the `ActivityGroup.name` union type and adding a single data entry — no new components or rendering changes needed.

## Technical Context

**Language/Version**: TypeScript 5.x + Next.js 16.0.7, React 19.2.1
**Primary Dependencies**: Tailwind CSS 3.4.18, NES.css 2.3.0
**Storage**: Static file (`src/content/portfolio.ts`) — no database
**Testing**: `pnpm lint` + `pnpm build` (no unit test framework yet)
**Target Platform**: Vercel (static/SSR)
**Project Type**: web (Next.js App Router)
**Performance Goals**: N/A (content-only change)
**Constraints**: No new dependencies, no rendering changes
**Scale/Scope**: 1 file changed, ~10 lines added

## Constitution Check

*GATE: Passed — no violations.*

- **Principle I (Personality-First)**: Real published articles strengthen the evidence surface. ✅
- **Principle II (Retro + Modern UX)**: No visual changes; uses existing responsive layout. ✅
- **Principle III (Content as Product)**: Real content, not placeholder. ✅
- **Principle IV (Lightweight)**: No new deps or components. ✅
- **Principle V (Docs Sync)**: CLAUDE.md to note expanded type if needed. ✅
- **Quality Gates**: `pnpm lint` and `pnpm build` must pass. ✅

## Project Structure

### Documentation (this feature)

```text
specs/041-add-codezine-series/
├── plan.md
├── spec.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── activity-group-type.md
└── tasks.md              (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
src/content/portfolio.ts   ← only file modified
```

**Structure Decision**: Single file content change. No new source files needed.

## Complexity Tracking

No violations — this is a minimal content + type change.
