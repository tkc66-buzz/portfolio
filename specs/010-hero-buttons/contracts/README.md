# Contracts

This feature is purely client-side navigation inside a single Next.js page.

- No API endpoints
- No backend services
- No contract tests

Verification is manual + quality gates:
- `pnpm lint`
- `pnpm build`


