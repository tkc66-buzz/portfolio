# Quickstart: Verify Menu navigation

## Run

```bash
pnpm install
pnpm dev
```

Open `http://localhost:3000` and verify:

1. Menu is visible under the Hero.
2. Menu buttons navigate to sections via hash.

## Quality gates

```bash
pnpm lint
pnpm build
```


