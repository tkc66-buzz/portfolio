# Contracts

This feature introduces **no API endpoints** and **no external integrations**.

- Data source remains `src/content/portfolio.ts` via `getPortfolio()`.
- UI state (selected quest) is client-only and not persisted.


